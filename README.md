# kafka_administration

Kafka HA realtime check + admin toolkit lives in **[`kafka_realtime_check/`](kafka_realtime_check/)**.

```bash
cd kafka_realtime_check
./check_kafka_ha.sh -c config/clusters/devkafka.env -u "$USER" -y
```
